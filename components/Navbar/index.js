import { Container, Body } from "./NavbarComponents";
const links = [
  { name: "Home", slug: "/" },
  { name: "Events", slug: "/events" },
  { name: "About", slug: "/about" },
  { name: "Contact", slug: "/contact" },
];

const Navbar = () => {
  return (
    <Container>
      <Body>sss</Body>
    </Container>
  );
};

export default Navbar;
